<%= form_for(@wechat) do |f| %>
  <% if @wechat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wechat.errors.count, "error") %> prohibited this wechat from being saved:</h2>

      <ul>
      <% @wechat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
